//! Intentionally empty: this crate exists only for its `tests/` directory,
//! which drives `dh-domain` headlessly through the same command/event API
//! the UIs use. The M0 loopback protocol sender will live here too.
